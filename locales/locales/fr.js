import * as common from "../common/fr.json";
import * as deliveries from "../deliveries/fr.json";

const translations = {
  common: common.default,
  deliveries: deliveries.default,
};

export default translations;
