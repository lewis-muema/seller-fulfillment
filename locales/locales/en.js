import * as common from "../common/en.json";
import * as deliveries from "../deliveries/en.json";
import * as payments from "../payments/en.json";

const translations = {
  common: common.default,
  deliveries: deliveries.default,
  payments: payments.default,
};

export default translations;
